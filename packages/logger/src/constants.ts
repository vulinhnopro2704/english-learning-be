export const TRACE_ID_HEADER = 'x-trace-id';

export const DEFAULT_REDACT_KEYS = [
  'password',
  'token',
  'access_token',
  'refresh_token',
  'cookie',
  'cookies',
];

export const DEFAULT_PREVIEW_LENGTH = 3;

export const DEFAULT_BODY_MAX = 0; // 0 means unlimited
